-------------화면분할 관련
:sp 분할해서_보고싶은_파일이름
 sp : 상하 분할
 vs : 좌우 분할

--------------문자열 관련

문자열 바꾸기(치환)

:%s/*/foo  // * 를 foo로 

:%s/*/foo/i  // 대소문자 구분 없이 바꿈

:%s/\<foo\>/s  // 정확히 *에 일치될 때만 바꿈
	       // foofoo foosss sssfoosss 이런 문자열의 foo는 바꾸지 않음
 

